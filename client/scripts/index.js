module.exports = function(config) {
  'use strict';

  var FFWD = config.FFWD;

  // var CodeMirror = require('codemirror');

  // FFWD.make('view', 'codeEditor', {
  //   tagName: 'textarea',

  //   initialize: function() {
  //     this.editor = CodeMirror.fromTextArea(this.el, {
  //       mode: 'markdown',
  //       lineNumbers: true,
  //       gutters: ['CodeMirror-linenumbers', 'breakpoints']
  //     });
  //   }
  // }, {});
};
